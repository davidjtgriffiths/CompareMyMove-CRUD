<form method="post" action="{{ path('app_company_matching_settings_delete', {'id': company_matching_setting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company_matching_setting.id) }}">
    <button class="btn">Delete</button>
</form>
